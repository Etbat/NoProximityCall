- name: Clone official Theos
  run: |
    git clone --recursive https://github.com/theos/theos.git ~/theos
    export THEOS=~/theos
    chmod +x ~/theos/bin/install-theos
    ~/theos/bin/install-theos
  env:
    THEOS: $HOME/theos
